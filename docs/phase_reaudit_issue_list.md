# 源码复审问题清单 (Phase Re-audit Issue List)

## 问题 1
- **问题编号**：ISSUE-001
- **阶段**：Phase 6
- **文件**：系统整体测试日志
- **严重等级**：P1
- **证据**：系统整体联调 (System Cascade) 代码逻辑正确，架构隔离充分，原子写入无误，但由于开发环境会话重置，前序联调过程产生的“串行链路高频日志”、“启停防抖测试日志”等运行态证据已丢失。
- **影响**：无法通过保留的原始 stdout 日志文件来 100% 验证防抖测试当时的毫秒级时序。
- **建议修复**：在下一次全局集成测试时，开启文件级的 debug log 追踪，持久化保存启动/停止/边界触发瞬间的时序日志。
- **是否阻塞验收**：否（当前全链路通过了最新的运行态覆盖测试）

## 问题 2
- **问题编号**：ISSUE-002
- **阶段**：全局
- **文件**：`walkthrough.md`, `README.md`
- **严重等级**：P2
- **证据**：前几轮高速开发中，文档部分未及时紧跟最终确定版的解耦架构 (特别是 Phase 7 架构回退)，导致 `walkthrough.md` 描述过早宣称 `paper_trade_engine.py` 已就绪，引发了本次核查。
- **影响**：业务文档与真实代码库的 Snapshot 存在滞后错位，可能误导后续开发者。
- **建议修复**：同步全盘文档。将最新的 `paper_signal_log.jsonl` 日志解耦架构更新到 `ARCHITECTURE.md` 与 `AI_TRADER_USER_MANUAL.md`。
- **是否阻塞验收**：否

## 问题 3
- **问题编号**：ISSUE-003
- **阶段**：全局
- **文件**：`ai_trader.py` (Dashboard)
- **严重等级**：P2
- **证据**：代码中实际作为 Dashboard 的文件是 `ai_trader.py`，但在部分沟通与日志中依然习惯性称呼为 `dashboard.py`。
- **影响**：造成文件寻址误解（如运行 `python -m py_compile dashboard.py` 会报错）。
- **建议修复**：在内部命名规范与 UI 文案中，统一正名为 `ai_trader.py` 或更名为 `dashboard.py`。
- **是否阻塞验收**：否
